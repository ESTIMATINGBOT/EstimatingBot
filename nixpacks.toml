[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "poppler_utils"]

[phases.install]
cmds = [
  "npm ci",
  "pip3 install anthropic reportlab pillow"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
