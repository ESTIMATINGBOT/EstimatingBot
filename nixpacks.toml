[phases.setup]
nixPkgs = ["python311", "poppler_utils"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install anthropic reportlab pillow"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
