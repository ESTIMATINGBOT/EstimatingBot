[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m pip install --user anthropic reportlab pillow pymupdf pdfplumber pikepdf"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
