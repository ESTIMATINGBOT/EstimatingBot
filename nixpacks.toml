[phases.setup]
nixPkgs = ["python3", "poppler"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m pip install --user anthropic reportlab pillow"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
